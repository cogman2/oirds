Each of these sub-directories contain the training prototxt and solver configurations.   All layers learn.
Each directory is a cut off of where layers are randomly initialized, rather than being initialized from the pre-trained state.
